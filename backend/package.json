{
  "name": "aredl-coordinator-backend",
  "private": true,
  "scripts": {
    "dev": "wrangler dev -c wrangler.toml",
    "deploy": "wrangler deploy -c wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
